		//Якушев++ Если доп сведения такие то выводим ОбластьСтрока жирной, если нет, то выводим обычный шрифт
		//так же если сведения не найдены,мы в этот блок не заходим, иначе вылезет ошибка обращении к полю наименования
		//Изначально тут было: ТабличныйДокумент.Присоединить(ОбластьТоварСтроки);	
		    Номенклатура=ВыборкаПоТоварам.Номенклатура;
			ОбШрифт = Новый Шрифт(, 10, Ложь, , , ) ;
			ЖШрифт = Новый Шрифт(, 10, Истина, , , ) ;
			//Задаем тип для доп сведения СкладскаяПрограмма
			СвойствоСкладскаяПрограмма = ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения.НайтиПоРеквизиту("Имя",																					"СкладскаяПрограмма");
			СвойствоПоставщик = ПланыВидовХарактеристик.ДополнительныеРеквизитыИСведения.НайтиПоРеквизиту("Имя", 
			"Поставщик");

			ДопСведСкладскаяПрограмма=УправлениеСвойствами.ЗначениеСвойства(Номенклатура, СвойствоСкладскаяПрограмма);
			ДопСведПоставщик=УправлениеСвойствами.ЗначениеСвойства(Номенклатура, СвойствоПоставщик);
			Если ДопСведПоставщик <>Неопределено И ДопСведСкладскаяПрограмма<>Неопределено Тогда	
				ДопСведСкладскаяПрограмма=ДопСведСкладскаяПрограмма.Наименование;
				ДопСведПоставщик=ДопСведПоставщик.Наименование;
				Если ДопСведСкладскаяПрограмма="Под заказ" И ДопСведПоставщик="КЕДР Столешницы"  Тогда		
					ОбластьТоварСтроки.Область().Шрифт=ЖШрифт;
					ТабличныйДокумент.Присоединить(ОбластьТоварСтроки);
				Иначе
					ОбластьТоварСтроки.Область().Шрифт=ОбШрифт;
					ТабличныйДокумент.Присоединить(ОбластьТоварСтроки);
				КонецЕсли;
			Иначе
				ТабличныйДокумент.Присоединить(ОбластьТоварСтроки);
			КонецЕсли;					
			//Якушев--
